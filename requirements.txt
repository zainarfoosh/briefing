beautifulsoup4>=4.12

# Only the provider you actually use needs to be installed; all three are listed
# so switching LLM_PROVIDER doesn't require touching this file.
cohere>=5.13
anthropic>=0.70
google-genai>=1.0
